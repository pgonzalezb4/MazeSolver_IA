[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
uvicorn = {extras = ["all"], version = "*"}
fastapi = {extras = ["all"], version = "*"}
python-multipart = "*"

[dev-packages]

[requires]
python_version = "3.10"
